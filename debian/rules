#!/usr/bin/make -f

export PYBUILD_NAME=ddupdate
export PYBUILD_BEFORE_BUILD=echo  INSTALLPATH: {destdir}/{install_dir}
export PYBUILD_AFTER_INSTALL= \
    sed -i 's|=.*/usr|= /usr|' {destdir}{install_dir}/ddupdate/install.conf

%:
	dh $@  --with python3 --buildsystem=pybuild
