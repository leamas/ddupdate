#!/bin/sh

set -e

case "$1" in
    configure)
        adduser --system --disabled-password --disabled-login \
             --home /var/lib/ddupdate --quiet \
             --force-badname --group ddupdate
        ;;
esac

#DEBHELPER#

exit 0
